services:
  novels:
    build: .
    container_name: novels
    ports:
      - "3000:3000"
    volumes:
      - ${HOST_DATA_DIR:-./data}:/app/data
    environment:
      - NODE_ENV=production
      - DATA_DIR=/app/data
    restart: unless-stopped
