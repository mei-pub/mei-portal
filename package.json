{
  "name": "mei-portal",
  "version": "0.1.0",
  "private": true,
  "description": "多应用聚合门户：全部应用为一等公民本地代码",
  "type": "module",
  "scripts": {
    "build:theme": "node scripts/build-theme.mjs",
    "gen:nginx": "node scripts/gen-nginx.mjs",
    "validate": "node scripts/validate-manifests.mjs",
    "build": "npm run validate && npm run build:theme && npm run gen:nginx"
  },
  "dependencies": {
    "ajv": "8.17.1",
    "js-yaml": "4.1.0"
  }
}
